#nullable enable

namespace PrimeFuncPack
{
    partial struct HttpSenderFailure
    {
        public static bool Equals(HttpSenderFailure left, HttpSenderFailure right)
            =>
            left.Equals(right);
    }
}